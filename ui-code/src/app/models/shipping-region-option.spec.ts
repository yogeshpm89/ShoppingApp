import { ShippingRegionOption } from './shipping-region-option';

describe('ShippingRegionOption', () => {
  it('should create an instance', () => {
    expect(new ShippingRegionOption()).toBeTruthy();
  });
});
